(defvar keys "")

(defwidget key_list []
  (literal :content keys )
)

;final eww window
(defwindow keystrokes
           :geometry (geometry :x "-10%"
                               :y "10%"
                               :anchor "top right")
           :wm-ignore true
(key_list))
